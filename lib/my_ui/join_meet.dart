import 'package:flutter/material.dart';

class JoinMeetUI extends StatelessWidget {
  const JoinMeetUI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Join Meeting')),
      body: Center(child: Text('Join Meeting UI will be implemented here')),
    );
  }
}
