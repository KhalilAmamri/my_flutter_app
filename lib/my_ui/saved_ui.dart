import 'package:flutter/material.dart';

class SavedUi extends StatelessWidget {
  const SavedUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Saved Items')));
  }
}
